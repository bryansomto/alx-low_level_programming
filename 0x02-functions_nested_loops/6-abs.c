#include "main.h"

/**
 * _abs - computes the absolute value of an integer
 *
 * @i: integer type parameter
 *
 * Return: returns 0
 */

int _abs(int)
{
	if (n < 0)
	{
		n = (-1) * n;
	}
	return (n);
}
